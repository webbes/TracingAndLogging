{
  "name": "tracing_and_logging",
  "version": "1.0.0",
  "description": "Tracing and Logging library",
  "main": "index.html",
  "dependencies": {
    "applicationinsights-js": "^1.0.9",
    "tslib": "^1.7.1"
  },
  "devDependencies": {
    "http-server": "^0.10.0",
    "typescript": "^2.3.4",
    "typings": "^2.1.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && typings install && tsc -w && http-server -a localhost -p 8080"
  },
  "author": "Wesley Bakker",
  "license": "ISC"
}
